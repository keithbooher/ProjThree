import React from 'react';



export const Select = props => (
    <select name="">
    <option value={`${props.option}`}>{props.option}</option>
    <option value={`${props.option}`}>{props.option}</option>
    <option value={`${props.option}`}>{props.option}</option>
    <option value={`${props.option}`}>{props.option}</option>
    </select>
  )